window.docContents[97].resolve({"/____________/______--do--_________/#do-elab-return-continue-break":{"contents":"do 表示法支持三个非本地跳转指令： return，提前终止整个 do 块； break，提前终止循环； continue，提前终止循环的单次迭代。\nreturn 始终是允许的，而 break 和 continue 仅在循环体内有效。\n在精化期间，这三个跳转中的每一个都由一个延续表示。\n\n\n\n\n\n\n\n这三个延续是使用帮助程序 enterLoopBody 安装在上下文中的。\n\nPrepare the context for elaborating the body of a loop.\nThis includes setting the return continuation, break continuation, continue continuation, as\nwell as the changed result type of the do block in the loop body.\n\nSingle-Iteration Loop单次迭代循环 once 执行一次其主体，跳至 break 或 continue 上的循环末尾：syntax (name := doOnce) \"once \" doSeq : doElem\n它的控制信息基于身体的控制信息。\nonce 永远不会中断或继续自身，因为它在其主体中处理 break 和 continue；因此，它将 breaks 和 continues 设置为 false。\nnumRegularExits 是控制可以到达 once 之后的代码的次数。\n主体的正常下降、break 和 continue 都将控制权转移到循环末尾，因此控制最多留下 once 一次。\n因此，当主体可以以这些方式中的任何一种退出时，numRegularExits 为 1，否则为 0，在这种情况下设置 noFallthrough。@[doElem_control_info doOnce]\ndef inferOnce : ControlInfoHandler := fun stx => do\n  let `(doElem| once $body) := stx | throwUnsupportedSyntax\n  let bodyInfo ← InferControlInfo.ofSeq body\n  let exits :=\n    bodyInfo.numRegularExits > 0 ||\n    bodyInfo.breaks ||\n    bodyInfo.continues\n  return { bodyInfo with\n    breaks := false\n    continues := false\n    numRegularExits := if exits then 1 else 0\n    noFallthrough := !exits\n  }\nonce 的实际精化器使用 enterLoopBody 将精化器的整体延续与主体内的 break 和 continue 延续关联起来。\n由于精心设计的主体可以从多个位置到达该延续，因此精化器计算了这些用途。\n主体的控制信息并不指示 break 和 continue 可以被调用多少次，因此它们近似为每个出口，安全地确保如果使用其中任何一个，则延续将被复制。\n总的近似使用计数被传递到 DoElemCont.withDuplicableCont，当使用计数大于 1 时，它会共享延续而不是在每次使用时重复它，从而避免代码爆炸。\n它直接从主体计算此计数，因为控制信息处理程序报告的值最多为 1 并且不反映内部使用的数量。@[doElem_elab doOnce]\ndef elabOnce : DoElab := fun stx dec => do\n  let `(doElem| once $body) := stx | throwUnsupportedSyntax\n  let dec ← dec.ensureUnit\n  let bodyInfo ← InferControlInfo.ofSeq body\n  let numRegularExits :=\n    bodyInfo.numRegularExits +\n    (if bodyInfo.breaks then 2 else 0) +\n    (if bodyInfo.continues then 2 else 0)\n  dec.withDuplicableCont { bodyInfo with numRegularExits } fun dec => do\n    let returnCont ← getReturnCont\n    let exitCont := dec.continueWithUnit\n    enterLoopBody exitCont exitCont returnCont do\n      elabDoSeq body dec\nonce 可用于终止计算的某些部分，而无需使用 return 终止整个 do 块：#eval show Id Nat from do\n  let mut x := 0\n  once\n    x := x + 2\n    if x % 2 = 0 then break\n    x := 0\n  return x\n2\n\n\n","context":"Lean 参考\u0009符号和宏\u0009扩展  do  表示法\u0009精化","header":"23.7.3.3. 控制流： return 、 break  和  continue","id":"/____________/______--do--_________/#do-elab-return-continue-break"},"/____________/______/#zh-basictypes-string-logical-root":{"contents":"A string is a sequence of Unicode scalar values.At runtime, strings are represented by dynamic arrays\nof bytes using the UTF-8 encoding. Both the size in bytes (String.utf8ByteSize) and in characters\n(String.length) are cached and take constant time. Many operations on strings perform in-place\nmodifications when the reference to the string is unique.The bytes of the UTF-8 encoding of the string. Since strings have a special representation in\nthe runtime, this function actually takes linear time and space at runtime. For efficient access\nto the string's bytes, use String.utf8ByteSize and String.getUTF8Byte.The bytes of the string form valid UTF-8.\n\nLean 中字符串的逻辑模型是一个包含两个字段的结构体：* String.toByteArray 是 ByteArray，其中包含字符串的 UTF-8 编码。* String.isValidUTF8 证明字节实际上是字符串的有效 UTF-8 编码。该模型允许使用字节数组上的操作来指定和证明低级别的字符串操作的属性，同时仍然建立在字节数组理论的基础上。\n同时，它足够接近真实的运行时表示，以避免逻辑模型与运行时表示中有意义的操作之间的阻抗不匹配。\n\n\n\n","context":"Lean 参考\u0009基本类型\u0009弦乐","header":"20.8.1. 逻辑模型","id":"/____________/______/#zh-basictypes-string-logical-root"}});