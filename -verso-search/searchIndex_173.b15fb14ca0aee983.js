window.docContents[173].resolve({"/IO/_________/#zh-io-h011":{"contents":"Seeds the random number generator state used by IO.rand.\n\nReturns a pseudorandom number between lo and hi, using and updating a saved random generator\nstate.This state can be seeded using IO.setRandSeed.\n\nGenerates a random Boolean.\n\nGenerates a random natural number in the interval [lo, hi].\n\n\n\n\n\n","context":"Lean 参考\u0009IO","header":"21.10. 随机数","id":"/IO/_________/#zh-io-h011"},"/______/#scoped-attributes":{"contents":"许多属性可以应用于特定范围。\n这决定了属性的效果是否仅在当前节范围、打开当前命名空间的命名空间中或在任何地方可见。\n这些范围指示还用于控制 语法扩展 和 类型类实例。\n每个属性负责精确定义这些术语对其特定效果的含义。\n\nAttribute Scopes每当建立全局范围的声明（默认）的 module 被传递导入时，全局范围的声明（默认）就会生效。\n它们通过缺少另一个范围修饰符来指示。本地范围的声明仅在建立它们的 节范围 范围内有效。只要打开建立作用域声明的 namespace，作用域声明就会生效。\n\n","context":"Lean 参考\u0009属性","header":"9.3. 范围属性","id":"/______/#scoped-attributes"},"/_________/_______________/#zh-iterators-h004":{"contents":"并非所有迭代器都保证返回有限数量的结果；迭代所有自然数是完全明智的。\n同样，并非所有迭代器都保证返回单个结果或终止；迭代器可以使用任意程序定义。\n因此，Lean 将迭代器分为三个终止类：* Finite 迭代器保证在有限数量的步骤后完成迭代。这些迭代器有一个 Finite 实例。* Productive 迭代器保证在有限多个步骤中产生一个值或终止，但它们可能产生无限多个值。这些迭代器有一个 Productive 实例。* 所有其他迭代器，其终止行为未知。这些迭代器都没有实例。所有有限迭代器都必然是高效的。\n\nFinite α m asserts that IterM (α := α) m terminates after finitely many steps. Technically,\nthis means that the relation of plausible successors is well-founded.\nGiven this typeclass, termination proofs for well-founded recursion over an iterator it can use\nit.finitelyManySteps as a termination measure.The relation of plausible successors is well-founded.\n\nProductive α m asserts that IterM (α := α) m terminates or emits a value after finitely many\nskips. Technically, this means that the relation of plausible successors during skips is\nwell-founded.\nGiven this typeclass, termination proofs for well-founded recursion over an iterator it can use\nit.finitelyManySkips as a termination measure.The relation of plausible successors during skips is well-founded.\n\nLean 的标准库提供了许多迭代迭代器的函数。这些消费者功能通常不\n对底层迭代器做出任何假设。特别是，对于某些迭代器，此类函数可能会永远运行。\n\n有时，函数确实终止是至关重要的。\n对于这些情况，组合器 Iter.ensureTermination 会产生一个迭代器，该迭代器提供保证终止的消费者变体。\n他们通常需要证明所涉及的迭代器是有限的。\n\nFor an iterator it, it.ensureTermination provides variants of consumers that always\nterminate.\n\nFor an iterator it, it.ensureTermination provides variants of consumers that always\nterminate.\n\nIterating Over Nat要编写一个依次生成每个自然数的迭代器，第一步是实现其内部状态。\n这个迭代器只需要记住下一个自然数：structure Nats where\n  next : Nat\n该迭代器只会产生下一个自然数。\n因此，其步进函数永远不会返回 skip 或 done。\n每当它产生一个值时，该值将是内部状态的 next 字段，并且后继迭代器的 next 字段将大 1。\ngrind策略足以表明该步骤确实合理：instance [Pure m] : Iterator Nats m Nat where\n  IsPlausibleStep it\n    | .yield it' n =>\n      n = it.internalState.next ∧\n      it'.internalState.next = n + 1\n    | _ => False\n  step it :=\n    let n := it.internalState.next\n    pure <| .deflate <|\n      .yield { it with internalState.next := n + 1 } n (by grind)\n每当定义迭代器时，都应提供 IteratorLoop 实例。\n对于 Iter.toList 或 for 循环等迭代器的大多数使用者来说，它们是必需的。\n人们可以使用它们的默认实现，如下所示：instance [Pure m] [Monad n] : IteratorLoop Nats m n :=\n  .defaultImplementation\n此 step 函数非常高效，因为它永远不会返回 skip。\n因此，skip 的每条链具有有限长度的证明可以依赖于以下事实：当 it 是 Nats 迭代器时，Iterator.IsPlausibleStep it (.skip it') = False：instance [Pure m] : Productive Nats m where\n  wf := .intro <| fun _ => .intro _ nofun\n因为有无限多个 Nat，所以迭代器不是有限的。可以使用以下函数创建 Nats 迭代器：def Nats.iter : Iter (α := Nats) Nat :=\n  IterM.mk { next := 0 } |>.toIter\n通过运行以下函数可以打印所有自然数：def f : IO Unit := do\n  for x in Nats.iter do\n    IO.println s!\"{x}\"\n该函数永远不会终止，按升序打印所有自然数，一个\n又一个。此迭代器对于 Iter.zip 等组合器最有用：#eval show IO Unit from do\n  let xs : List String := [\"cat\", \"dog\", \"pachycephalosaurus\"]\n  for (x, y) in Nats.iter.zip xs.iter do\n    IO.println s!\"{x}: {y}\"\n0: cat\n1: dog\n2: pachycephalosaurus\n与前面的示例相反，此循环终止，因为 xs.iter 是有限迭代器，\n通过提供 Finite 实例，可以确保循环确实终止：#check type_of% (Nats.iter.zip [\"cat\", \"dog\"].iter).internalState\n\n#synth Finite (Zip Nats Id (ListIterator String) String) Id\nZip Nats Id (ListIterator String) String : Type\nZip.instFinite₂\n相反，Nats.iter 没有 Finite 实例，因为它产生无限多个值：#synth Finite Nats Id\nfailed to synthesize\n  Finite Nats Id\n\nHint: Additional diagnostic information may be available using the `set_option diagnostics true` command.\n因为有无限多个 Nat，所以使用 Iter.ensureTermination 会导致错误：#eval show IO Unit from do\n  for x in Nats.iter.ensureTermination do\n    IO.println s!\"{x}\"\nfailed to synthesize instance for 'for_in%' notation\n  ForIn (EIO IO.Error) (Iter.Total Nat) ?m.12\n\n\nIterating Over Triples类型 Triple 包含三个相同类型的值：structure Triple α where\n  fst : α\n  snd : α\n  thd : α\nTriple 上的迭代器的内部状态可以由与当前位置配对的三元组组成。\n该位置可以是字段之一，也可以是迭代完成的指示。inductive TriplePos where\n  | fst | snd | thd | done\n位置可用于查找元素：def Triple.get? (xs : Triple α) (pos : TriplePos) : Option α :=\n  match pos with\n  | .fst => some xs.fst\n  | .snd => some xs.snd\n  | .thd => some xs.thd\n  | _ => none\n每个字段的位置都有一个后继位置：@[grind, grind cases]\ninductive TriplePos.Succ : TriplePos → TriplePos → Prop where\n  | fst : Succ .fst .snd\n  | snd : Succ .snd .thd\n  | thd : Succ .thd .done\n迭代器本身将一个三元组与下一个元素的位置配对：structure TripleIterator α where\n  triple : Triple α\n  pos : TriplePos\n迭代从 fst 开始：def Triple.iter (xs : Triple α) : Iter (α := TripleIterator α) α :=\n  IterM.mk {triple := xs, pos := .fst : TripleIterator α} |>.toIter\n有两个看似合理的步骤：要么迭代器的位置有后继者，在这种情况下，下一个迭代器是与后继者位置指向相同三元组的迭代器，要么没有，在这种情况下迭代完成。@[grind]\ninductive TripleIterator.IsPlausibleStep :\n    @IterM (TripleIterator α) m α →\n    IterStep (@IterM (TripleIterator α) m α) α →\n    Prop where\n  | yield :\n    it.internalState.triple = it'.internalState.triple →\n    it.internalState.pos.Succ it'.internalState.pos →\n    it.internalState.triple.get? it.internalState.pos = some out →\n    IsPlausibleStep it (.yield it' out)\n  | done :\n    it.internalState.pos = .done →\n    IsPlausibleStep it .done\n相应的步骤函数产生由以下关系描述的迭代器和值：instance [Pure m] : Iterator (TripleIterator α) m α where\n  IsPlausibleStep := TripleIterator.IsPlausibleStep\n  step\n    | ⟨xs, pos⟩ =>\n      pure <| .deflate <|\n      match pos with\n      | .fst => .yield ⟨xs, .snd⟩ xs.fst ?_\n      | .snd => .yield ⟨xs, .thd⟩ xs.snd ?_\n      | .thd => .yield ⟨xs, .done⟩ xs.thd ?_\n      | .done => .done <| ?_\nwhere finally\n  all_goals grind [Triple.get?]\n现在可以将该迭代器转换为数组：def abc : Triple Char := ⟨'a', 'b', 'c'⟩\n#eval abc.iter.toArray\n#['a', 'b', 'c']\n一般来说，Iter.toArray 可能会永远运行。可以证明 abc 是有限的，上面的例子将在有限多个步骤后终止，通过\n构造 Finite (Triple Char) Id 实例。\n最简单的方法是从 TriplePos.done 开始，向后推向 TriplePos.fst，从而显示每个位置依次具有有限的后继链：@[grind! .]\ntheorem acc_done [Pure m] :\n    Acc (IterM.IsPlausibleSuccessorOf (m := m))\n      ⟨{ triple, pos := .done : TripleIterator α}⟩ :=\n  Acc.intro _ fun\n    | _, ⟨_, ⟨_, h⟩⟩ => by\n      cases h <;> grind [IterStep.successor_done]\n\n@[grind! .]\ntheorem acc_thd [Pure m] :\n    Acc (IterM.IsPlausibleSuccessorOf (m := m))\n      ⟨{ triple, pos := .thd : TripleIterator α}⟩ :=\n  Acc.intro _ fun\n    | ⟨{ triple, pos }⟩, ⟨h, h', h''⟩ => by\n      cases h'' <;> grind [IterStep.successor_yield]\n\n@[grind! .]\ntheorem acc_snd [Pure m] :\n    Acc (IterM.IsPlausibleSuccessorOf (m := m))\n      ⟨{ triple, pos := .snd : TripleIterator α}⟩ :=\n  Acc.intro _ fun\n    | ⟨{ triple, pos }⟩, ⟨h, h', h''⟩ => by\n      cases h'' <;> grind [IterStep.successor_yield]\n\n@[grind! .]\ntheorem acc_fst [Pure m] :\n    Acc (IterM.IsPlausibleSuccessorOf (m := m))\n      ⟨{ triple, pos := .fst : TripleIterator α}⟩ :=\n  Acc.intro _ fun\n    | ⟨{ triple, pos }⟩, ⟨h, h', h''⟩ => by\n      cases h'' <;> grind [IterStep.successor_yield]\n\ninstance [Pure m] : Finite (TripleIterator α) m where\n  wf := .intro <| fun\n    | { internalState := { triple, pos } } => by\n      cases pos <;> grind\n要在 for 循环中启用迭代器，需要 IteratorLoop 的实例：instance [Monad m] [Monad n] :\n    IteratorLoop (TripleIterator α) m n :=\n  .defaultImplementation\n#eval show IO Unit from do\n  for x in abc.iter do\n    IO.println x\na\nb\nc\n\n\nIterators and Effects迭代文件内容的一种方法是在每一步从 Stream 读取指定数量的字节。\n当到达 EOF 时，迭代器可以通过让其引用计数降至零来关闭文件：structure FileIterator where\n  stream? : Option IO.FS.Stream\n  count : USize := 8192\n可以通过打开文件并将其句柄转换为流来创建迭代器：def iterFile\n    (path : System.FilePath)\n    (count : USize := 8192) :\n    IO (IterM (α := FileIterator) IO ByteArray) := do\n  let h ← IO.FS.Handle.mk path .read\n  let stream? := some (IO.FS.Stream.ofHandle h)\n  return IterM.mk { stream?, count }\n对于此迭代器，当文件仍然打开时，yield 是合理的，而当文件关闭时，done 是合理的。\n实际的步骤函数执行读取并在没有返回字节的情况下关闭文件：instance : Iterator FileIterator IO ByteArray where\n  IsPlausibleStep it\n    | .yield .. =>\n      it.internalState.stream?.isSome\n    | .skip .. => False\n    | .done => it.internalState.stream?.isNone\n  step it := do\n    let { stream?, count } := it.internalState\n    match stream? with\n    | none => return .deflate <| .done rfl\n    | some stream =>\n      let bytes ← stream.read count\n      let it' :=\n        { it with internalState.stream? :=\n          if bytes.size == 0 then none else some stream\n        }\n      return .deflate <| .yield it' bytes (by grind)\n要在循环中使用它，需要 IteratorLoop 实例。instance [Monad n] : IteratorLoop FileIterator IO n :=\n  .defaultImplementation\n这是足够的支持代码来使用迭代器来计算文件大小：def fileSize (name : System.FilePath) : IO Nat := do\n  let mut size := 0\n  let f := (← iterFile name)\n  for bytes in f do\n    size := size + bytes.size\n  return size\n\n\n","context":"Lean 参考\u0009迭代器\u0009迭代器定义","header":"22.2.2. 有限且高效的迭代器","id":"/_________/_______________/#zh-iterators-h004"},"/_________/_______________/#zh-iterators-h013":{"contents":"Iterates over the whole iterator, applying the monadic effects of each step, discarding all\nemitted values.\n\nFolds a function over an iterator from the left, accumulating a value starting with init.\nThe accumulated value is combined with the each element of the list in order, using f.It is equivalent to it.toList.foldl.\n\nFolds a monadic function over an iterator from the left, accumulating a value starting with init.\nThe accumulated value is combined with the each element of the list in order, using f.The monadic effects of f are interleaved with potential effects caused by the iterator's step\nfunction. Therefore, it may not be equivalent to (← it.toList).foldlM.\n\nSteps through the whole iterator, counting the number of outputs emitted.Performance:This function's runtime is linear in the number of steps taken by the iterator.\n\nReturns ULift.up true if the pure predicate p returns true for\nany element emitted by the iterator it.O(|it|). Short-circuits upon encountering the first match. The outputs of it are\nexamined in order of iteration.\n\nReturns ULift.up true if the monadic predicate p returns ULift.up true for\nany element emitted by the iterator it.O(|it|). Short-circuits upon encountering the first match. The outputs of it are\nexamined in order of iteration.\n\nReturns ULift.up true if the pure predicate p returns true for\nall elements emitted by the iterator it.O(|it|). Short-circuits upon encountering the first mismatch. The outputs of it are\nexamined in order of iteration.If the iterator is not finite, this function might run forever. The variant\nit.ensureTermination.toListRev always terminates after finitely many steps.\n\nReturns ULift.up true if the monadic predicate p returns ULift.up true for\nall elements emitted by the iterator it.O(|it|). Short-circuits upon encountering the first mismatch. The outputs of it are\nexamined in order of iteration.\n\nReturns the first output of the iterator for which the predicate p returns true, or none if\nno such output is found.O(|it|). Short-circuits upon encountering the first match. The elements in it are examined in\norder of iteration.If the iterator is not finite, this function might run forever. The variant\nit.ensureTermination.find? always terminates after finitely many steps.Examples:* ([7, 6, 5, 8, 1, 2, 6].iterM Id).find? (· < 5) = pure (some 1)* ([7, 6, 5, 8, 1, 2, 6].iterM Id).find? (· < 1) = pure none\n\nReturns the first output of the iterator for which the monadic predicate p returns true, or\nnone if no such element is found.O(|it|). Short-circuits when f returns true. The outputs of it are examined in order of\niteration.If the iterator is not finite, this function might run forever. The variant\nit.ensureTermination.findM? always terminates after finitely many steps.Example:#eval ([7, 6, 5, 8, 1, 2, 6].iterM IO).findM? fun i => do\n  if i < 5 then\n    return true\n  if i ≤ 6 then\n    IO.println s!\"Almost! {i}\"\n  return false\nAlmost! 6\nAlmost! 5\nsome 1\n\n\nReturns the first non-none result of applying f to each output of the iterator, in order.\nReturns none if f returns none for all outputs.O(|it|). Short-circuits when f returns some _.The outputs of it are examined in order of\niteration.If the iterator is not finite, this function might run forever. The variant\nit.ensureTermination.findSome? always terminates after finitely many steps.Examples:* ([7, 6, 5, 8, 1, 2, 6].iterM Id).findSome? (fun x => if x < 5 then some (10 * x) else none) = pure (some 10)* ([7, 6, 5, 8, 1, 2, 6].iterM Id).findSome? (fun x => if x < 1 then some (10 * x) else none) = pure none\n\nReturns the first non-none result of applying the monadic function f to each output\nof the iterator, in order. Returns none if f returns none for all outputs.O(|it|). Short-circuits when f returns some _. The outputs of it are\nexamined in order of iteration.If the iterator is not finite, this function might run forever. The variant\nit.ensureTermination.findSomeM? always terminates after finitely many steps.Example:#eval ([7, 6, 5, 8, 1, 2, 6].iterM IO).findSomeM? fun i => do\n  if i < 5 then\n    return some (i * 10)\n  if i ≤ 6 then\n    IO.println s!\"Almost! {i}\"\n  return none\nAlmost! 6\nAlmost! 5\nsome 10\n\n\nReturns the n-th value emitted by it, or none if it terminates earlier.For monadic iterators, the monadic effects of this operation may differ from manually iterating\nto the n-th value because atIdx? can take shortcuts. By the signature, the return value\nis guaranteed to plausible in the sense of IterM.IsPlausibleNthOutputStep.This function is only available for iterators that explicitly support it by implementing\nthe IteratorAccess typeclass.\n\n","context":"Lean 参考\u0009迭代器\u0009使用迭代器","header":"22.3.3. 使用 Monadic 迭代器","id":"/_________/_______________/#zh-iterators-h013"},"/____________/______/#zh-basictypes-array-subarray-h002":{"contents":"Removes the first i elements of the subarray. If there are i or fewer elements, the resulting\nsubarray is empty.\n\nKeeps only the first i elements of the subarray. If there are i or fewer elements, the resulting\nsubarray is empty.\n\nShrinks the subarray by incrementing its starting index if possible, returning it unchanged if not.Examples:* #[1,2,3].toSubarray.popFront.toArray = #[2, 3]* #[1,2,3].toSubarray.popFront.popFront.toArray = #[3]* #[1,2,3].toSubarray.popFront.popFront.popFront.toArray = #[]* #[1,2,3].toSubarray.popFront.popFront.popFront.popFront.toArray = #[]\n\nSplits a subarray into two parts, the first of which contains the first i elements and the second\nof which contains the remainder.\n\n","context":"Lean 参考\u0009基本类型\u0009储备\u0009子储备","header":"20.16.5.2. 调整大小","id":"/____________/______/#zh-basictypes-array-subarray-h002"},"/____________/_________/#zh-basictypes-option-h001":{"contents":"\n\n有一个从 α 到 Option α 的 强制 将值包装在 some 中。\n这允许 Option 的使用方式与其他语言中的可空类型类似，其中缺少的值由 none 指示，而存在的值没有特别标记。\n\nCoercions and Option在getAlpha中，读取一行输入。\n如果该行仅由字母组成（从其开头和结尾删除空格后），则返回该行；否则，函数返回 none。def getAlpha : IO (Option String) := do\n  let line := (← (← IO.getStdin).getLine).trim\n  if line.length > 0 && line.all Char.isAlpha then\n    return line\n  else\n    return none\n在成功的情况下，line 周围没有显式的 some。\nsome 通过强制自动插入。\n\n\n\n","context":"Lean 参考\u0009基本类型\u0009任选值","header":"20.12.1. 强制","id":"/____________/_________/#zh-basictypes-option-h001"},"/grind-______/_____________________/#grind-linarith-classes":{"contents":"要向 linarith 添加对新类型的支持，第一步是在可能的情况下实现 IntModule，否则实现 NatModule。\n每个 Ring 都已经是 IntModule，并且每个 Semiring 都已经是 NatModule，因此实现这些实例之一也足够了。\n接下来，应实现订单类之一（Std.IsPreorder、Std.IsPartialOrder 或 Std.IsLinearOrder）。\n通常，当上下文已包含矛盾时，IsPreorder 实例就足够了，但需要 IsLinearOrder 实例才能证明线性不等式目标。\n通过实现 OrderedAdd 和 OrderedRing 来启用其他功能，OrderedAdd 表示模块中的加法结构与阶数兼容，OrderedRing 改进了对常量的支持。\n\nA module over the natural numbers, i.e. a type with zero, addition, and scalar multiplication by natural numbers,\nsatisfying appropriate compatibilities.Equivalently, an additive commutative monoid.Use IntModule if the type has negation.Scalar multiplication by natural numbers.Scalar multiplication by zero is zero.Scalar multiplication by a successor.\n\nA module over the integers, i.e. a type with zero, addition, negation, subtraction, and scalar multiplication by integers,\nsatisfying appropriate compatibilities.Equivalently, an additive commutative group.Scalar multiplication by natural numbers.Scalar multiplication by integers.Scalar multiplication by zero is zero.Scalar multiplication by one is the identity.Scalar multiplication is distributive over addition in the integers.Scalar multiplication by natural numbers is consistent with scalar multiplication by integers.\n\nAddition is compatible with a preorder if a ≤ b ↔ a + c ≤ b + c.a + c ≤ b + c iff a ≤ b.\n\nA ring which is also equipped with a preorder is considered a strict ordered ring if addition, negation,\nand multiplication are compatible with the preorder, and 0 < 1.In a strict ordered semiring, we have 0 < 1.In a strict ordered semiring, we can multiply an inequality a < b on the left\nby a positive element 0 < c to obtain c * a < c * b.In a strict ordered semiring, we can multiply an inequality a < b on the right\nby a positive element 0 < c to obtain a * c < b * c.\n\n","context":"Lean 参考\u0009grind 策略\u0009线性算术工作站","header":"16.9.1. 支持 linarith","id":"/grind-______/_____________________/#grind-linarith-classes"}});